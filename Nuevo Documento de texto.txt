<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Mis Cupones | La Sastrería</title>
  <link rel="stylesheet" href="assets/css/style.css">
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
  <style>
    body {
      background: url("assets/img/fondo-hexagonos.jpg") no-repeat center center fixed;
      background-size: cover;
      font-family: "Poppins", sans-serif;
      color: #fff;
      display: flex;
      justify-content: center;
      align-items: flex-start;
      min-height: 100vh;
      padding-top: 40px;
      overflow-x: hidden;
    }

    .overlay {
      position: fixed;
      inset: 0;
      background: rgba(0, 0, 0, 0.75);
      z-index: 0;
    }

    .contenedor {
      position: relative;
      z-index: 2;
      background: rgba(0, 0, 0, 0.85);
      border-radius: 25px;
      padding: 30px;
      width: 95%;
      max-width: 600px;
      box-shadow: 0 0 30px rgba(0,255,242,0.4);
      text-align: center;
    }

    h1 {
      color: #00fff2;
      font-size: 1.8em;
      text-shadow: 0 0 15px #00fff2;
      margin-bottom: 15px;
    }

    .cupon {
      background: rgba(255,255,255,0.05);
      border: 2px dashed #00fff2;
      border-radius: 15px;
      padding: 15px;
      margin-bottom: 20px;
      box-shadow: 0 0 10px rgba(0,255,242,0.3);
      display: flex;
      flex-wrap: wrap;
      justify-content: space-between;
      align-items: center;
    }

    .cupon-info {
      flex: 1;
      text-align: left;
      padding-right: 10px;
    }

    .cupon h2 {
      color: #00fff2;
      font-size: 1.2em;
      margin-bottom: 5px;
    }

    .cupon p {
      color: #ccc;
      font-size: 0.9em;
      margin-bottom: 5px;
    }

    .qr {
      background: #fff;
      border-radius: 10px;
      padding: 6px;
      box-shadow: 0 0 15px rgba(0,255,242,0.4);
      width: 100px;
      height: 100px;
      object-fit: contain;
    }

    .estado-usado {
      opacity: 0.4;
      filter: grayscale(1);
    }

    .btn-volver {
      display: inline-block;
      margin-top: 15px;
      color: #00fff2;
      text-decoration: none;
      font-weight: 600;
      transition: 0.3s;
    }

    .btn-volver:hover {
      text-shadow: 0 0 15px #00fff2;
    }
  </style>
</head>
<body>
  <div class="overlay"></div>

  <div class="contenedor">
    <h1>🎟️ Mis Cupones</h1>
    <div id="listaCupones"></div>
    <a href="inicio.html" class="btn-volver">← Volver al inicio</a>
  </div>

  <script>
    const idUsuario = localStorage.getItem("id_usuario");

    if (!idUsuario) {
      alert("⚠️ No has iniciado sesión.");
      window.location.href = "login.html";
    }

    async function cargarCupones() {
      try {
        const res = await fetch(`php/obtener_cupones.php?id_usuario=${idUsuario}`);
        const data = await res.json();

        const lista = document.getElementById("listaCupones");
        lista.innerHTML = "";

        if (data.status === "ok" && data.cupones.length > 0) {
          data.cupones.forEach(c => {
            const estadoClass = c.usado == 1 ? "estado-usado" : "";
            const qrURL = `https://api.qrserver.com/v1/create-qr-code/?size=150x150&data=${encodeURIComponent(c.codigo_unico)}`;

            lista.innerHTML += `
              <div class="cupon ${estadoClass}">
                <div class="cupon-info">
                  <h2>${c.nombre}</h2>
                  <p>${c.descripcion}</p>
                  <p><strong>Tipo:</strong> ${c.tipo}</p>
                  <p><strong>Código:</strong> ${c.codigo_unico}</p>
                </div>
                <img src="${qrURL}" class="qr" alt="QR de cupón">
              </div>
            `;
          });
        } else {
          lista.innerHTML = "<p>No tenés cupones asignados todavía.</p>";
        }

      } catch (error) {
        alert("⚠️ Error al cargar cupones.");
      }
    }

    cargarCupones();
  </script>
</body>
</html>
